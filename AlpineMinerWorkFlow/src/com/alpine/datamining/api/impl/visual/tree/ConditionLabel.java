package com.alpine.datamining.api.impl.visual.tree;

import org.eclipse.draw2d.Label;

public class ConditionLabel extends Label {
	
	ConditionLabel(String label) {
		super(label);
		setOpaque(true);
		//setBackgroundColor(ColorConstants.buttonLightest); 
		//setBorder(new LineBorder());
	}
}